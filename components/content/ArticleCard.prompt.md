A single clip in the work grid — cover image, type tag, category eyebrow, uppercase headline, outlet/date. Hover pops a hard offset shadow.

```jsx
<ArticleCard kind="article" category="Football" title="Cowboys stun No. 12 Longhorns" outlet="The O'Colly" date="Oct 2025" image="../assets/cover.jpg" />
```

`kind`: `article` / `video` / `social`.
