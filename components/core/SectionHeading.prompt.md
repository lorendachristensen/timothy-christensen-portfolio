Section title with kicker eyebrow and the signature 2px black underline rule.

```jsx
<SectionHeading kicker="Latest" title="Recent Work" action={<Button variant="ghost" size="sm">All clips</Button>} />
```
