Content-type / topic label chip — mono, uppercase, square, 2px border.

```jsx
<Tag kind="video">▶ Video</Tag>
<Tag kind="article">Article</Tag>
<Tag kind="topic">Football</Tag>
```

`kind`: `article` (black) · `video` (orange) · `social` (outline) · `topic` (muted outline).
